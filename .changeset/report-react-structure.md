---
"nestjs-doctor": patch
---

The report UI source collapses into the React app: the pure logic modules move to app/lib, the icon data and legend live in the app, and the last string atoms are gone. The emitted page is unchanged.
