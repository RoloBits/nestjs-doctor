---
"nestjs-doctor": patch
---

The HTML report's header, tab bar and share dialog are now rendered by React; the RPT helper bundle is gone from the page, since every consumer imports the modules directly.
