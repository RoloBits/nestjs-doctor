---
"nestjs-doctor": minor
---

The HTML report reports five named interactions alongside the tab it was
opened on: the rule lab being run, a module opened from a finding, the graph
recentered, the boot trace opened, and the module tree expanded. Each is a
fixed name from a list in the beacon, so no element text, file path, or class
name from the report ever travels.
