---
"nestjs-doctor": minor
---

Add `--report-ui` (and a `report.ui` config key) to render the HTML report with the React shell instead of the legacy template. Default stays legacy; the React variant is self-contained and loads no external scripts.
