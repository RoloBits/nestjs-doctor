---
"nestjs-doctor": patch
---

Repoint the two README links that pointed into anchors on the docs Setup page, which is now a quickstart. Agent setup moved to `/docs/coding-agents` and the Node API to `/docs/reference/node-api`. Also fixes the link in the generated HTML report's rule playground, which pointed at an anchor that never existed.
